# J'ai gardé tous les elements du fichier de configuration du serveur mariaDB (appelé 50-server.cnf qui se trouve dans etc/mysql/mariadb.conf.d/50-server.cnf) j'ai juste modifier les lignes ports et bind-address)


[server]
[mysqld]
user                    = root
pid-file                = /run/mysqld/mysqld.pid
socket                  = /run/mysqld/mysqld.sock
# J'ai decommenté le port, initiallement commenté: Permet de preciser sur quel port on veut communiquer (demandé dans le sujet) 
port                    = 3306
basedir                 = /usr
datadir                 = /var/lib/mysql
tmpdir                  = /tmp
lc-messages-dir         = /usr/share/mysql
# J'ai modifié bind-address, initiallement 127.0.0.1: Permet de preciser que toutes les IP du réseau peuvent se connecter et pas seulement 127.0.0.1 (car le client mariadb se trouve dans notre conteneur wordpress avec une IP differente de localhost mais sur le meme reseau: pour verifier faire un ifconfig dans le conteneur wordpress pour connaitre son ip)
bind-address            =*
query_cache_size        = 16M
log_error = /var/log/mysql/error.log
expire_logs_days        = 10
character-set-server  = utf8mb4
collation-server      = utf8mb4_general_ci
[embedded]
[mariadb]
[mariadb-10.3]
